# Production — set in Netlify dashboard → Environment variables
NEXT_PUBLIC_STRAPI_URL=https://cms.yourdomain.com

# OpenAI — server-side only (do NOT prefix with NEXT_PUBLIC_)
OPENAI_API_KEY=sk-your-key-here
